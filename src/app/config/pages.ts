import { Page } from '../types';
import { routes } from './routes';

export const pages: Array<Page> = [
  {
    id: 'cities',
    title: 'Cities',
    path: routes.cities,
    icon: 'cities',
  },
];
